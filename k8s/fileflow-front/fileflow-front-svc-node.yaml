apiVersion: v1
kind: Service
metadata:
    name: fileflow-front-svc-nodeport
    namespace: file-flow
spec:
    type: NodePort
    selector:
        app: fileflow-front
    ports:
        -   protocol: TCP
            port: 80
            targetPort: 80
            nodePort: 30000
